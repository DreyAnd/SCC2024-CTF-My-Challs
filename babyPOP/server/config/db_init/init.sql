CREATE DATABASE IF NOT EXISTS babyPOP;
CREATE USER 'babypop'@'localhost' IDENTIFIED BY '2?DtQ`4%J_p:4chq';
GRANT CREATE, ALTER, DROP, INSERT, UPDATE, DELETE, SELECT, REFERENCES, RELOAD on *.* TO 'babypop'@'localhost' WITH GRANT OPTION;
FLUSH PRIVILEGES;

USE babyPOP;

CREATE TABLE IF NOT EXISTS users (
    id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
